import styled from 'styled-components';
import { MapContainer } from 'react-leaflet';


const ContactsMap = styled(MapContainer)`
  display: flex;
  width: 649px;
  height: 336px;
  margin-left: auto;
`;

export { ContactsMap };
